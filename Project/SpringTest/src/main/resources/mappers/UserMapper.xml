<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zerock.test.mapper.UserMapper">
	<insert id="insertUser" parameterType="com.zerock.test.dto.UserDTO">
		INSERT INTO USERINFO (id, pwd, name, email, phone, street_address, detail_address, gender, birthdate)
		VALUES (#{id}, #{pwd}, #{name}, #{mail}, #{number}, #{Street_address}, #{detail_address}, #{gender}, #{birthDate})
	</insert>

</mapper>